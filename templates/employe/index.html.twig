{% extends 'base.html.twig' %}

{% block title %}Liste des employés{% endblock %}

{% block body %}
    <h1>Liste des employés</h1>

    {% for employe in employes %}
    <b> <a href="{{ path('show_employe', {'id': employe.id}) }}">{{employe}}</a> ({{employe.entreprise.raisonSociale}}) </b> <br>
    {% endfor %}
{% endblock %}
